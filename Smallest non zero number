class Solution {
  public:
    int find(vector<int>& arr) {
        // code here
        int c=0;
        for(int i=arr.size()-1;i>=0;i--){
            c+=arr[i];
            c=(c+1)/2;
        }
        return c;
    }
};
